# Per-change context that biases planning, principle selection, and infra routing.
# Generated by: specpaper propose
# Inherits defaults from .specpaper/project.yaml; CTO updates during plan.
customer_tier: "internal"          # "enterprise" | "smb" | "internal"
compliance: []                     # e.g. ["sox", "hipaa", "gdpr", "pci"]
deployment_target: ""              # "azure" | "hetzner" — set by CTO during plan
data_residency: ""                 # e.g. "eu-west", "us-east", "any"
notes: ""
